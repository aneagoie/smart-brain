# SmartBrain - v2
Final project for Udemy course

## Port
with imprvoments

1. Clone this repo
2. Run `npm install`
3. Run `npm start`

## Note for code analysis
First start with package.json to figure out what the layout of a project as and dependencies.

`createdb 'DBNAME'` from brew installed version of psql.